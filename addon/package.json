{
	"name": "loop-addon",
	"version": "1.0.0",
	"description": "",
	"main": "./Build/Release/loop.node",
	"scripts": {
		"clean": "node-gyp clean",
		"configure": "node-gyp configure",
		"build": "node-gyp build",
		"rebuild": "npm run clean && npm run configure && npm run build",
		"setup": "npm run configure && npm run build"
	},
	"author": "Johnny Bakker",
	"license": "ISC",
	"devDependencies": {
		"@types/node": "^14.14.6",
		"del-cli": "^3.0.1",
		"node-gyp": "^7.1.2",
		"ts-node": "^9.0.0",
		"typescript": "^4.0.5"
	}
}
